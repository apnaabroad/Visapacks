# SQLite is used by default for zero-setup local development.
# To use Postgres instead, set e.g. postgresql://user:pass@localhost:5432/visapacks
# and change the "provider" in prisma/schema.prisma from "sqlite" to "postgresql".
DATABASE_URL="file:./dev.db"

PORT=4000
CORS_ORIGIN="http://localhost:5173"
